#include <stdio.h>

int main() {
    printf("char is of size %ld\n", sizeof(char));
    printf("short is of size %ld\n", sizeof(short));
    printf("int is of size %ld\n", sizeof(int));
    printf("long is of size %ld\n", sizeof(long));
}

